build:
  frontend: Frontend/Dockerfile
  backend: Backend/Dockerfile

release:
  command:
    - npm run migrate
    - npm run seed

run:
  - type: web
    command: npm start
    name: Frontend

  - type: web
    command: npm start
    name: Backend

  - type: worker
    command: node worker.js
    name: Worker
