build:
  docker:

    frontend: Frontend/Dockerfile
    backend: backend/Dockerfile

release:
  command:
    - npm run migrate
    - npm run seed

run:
  web: npm start
  backend: npm start
  worker: node worker.js
