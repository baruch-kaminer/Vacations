build:
  frontend: Frontend/
  backend: Backend/

release:
  command:
    - npm run migrate
    - npm run seed

run:
  - type: web
    command: npm start
    name: Frontend

  - type: web
    command: npm start
    name: Backend

  - type: worker
    command: node worker.js
    name: Worker
