build:
  docker:

    Frontend: Frontend/Dockerfile
    Backend: Backend/Dockerfile

release:
  command:
    - npm run migrate
    - npm run seed

run:
  web:
    Frontend:
      - npm start
    image: vacations-frontend

    Backend:
      command:
        - npm start
      image: vacations-backend