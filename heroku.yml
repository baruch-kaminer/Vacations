build:
  docker:
    web: frontend/Dockerfile
    backend: backend/Dockerfile

release:
  command:
    - npm run migrate
    - npm run seed

run:
  web: npm start
  backend: npm start
  worker:
    command:
      - node worker.js
    image: vacations
