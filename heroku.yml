build:
  frontend:
    docker:
      web: Frontend/Dockerfile
  backend:
    docker:
      web: Backend/Dockerfile

run:
  - type: web
    command: npm start
    name: Frontend

  - type: web
    command: npm start
    name: Backend

  - type: worker
    command: node worker.js
    name: Worker

addons:
  - plan: heroku-mysql
    as: DATABASE

